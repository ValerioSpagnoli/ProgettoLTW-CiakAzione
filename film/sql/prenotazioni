create table prenotazioni (
	idPrenotazione numeric,
	email varchar(50),
	titolo varchar (100),
	orario varchar (10),
	cinema varchar (50),
	sala varchar(10),
	postiPrenotati varchar(10),
	primary key (idPrenotazione)
)